package com.github.zerokode.objects.attributes;

import com.github.zerokode.objects.location.Direction;

/**
 * A flying object can't collide with objects placed on the map or holes in the ground,
 * but it can collide with enemies.
 */
public class Flying extends Movable {

    public void fly(Direction direction){

    }

}
